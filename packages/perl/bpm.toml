[package]
name     = "perl"
version  = "5.40.4"
release  = 1
summary  = "A highly capable, feature-rich programming language"
homepage = "https://www.perl.org/"
license  = ["GPL-1.0-or-later", "Artistic-1.0-Perl"]
arch     = ["x86_64"]

depends = ["glibc", "gdbm", "libxcrypt"]

[[source]]
# 5.40.4 fixes CVE-2026-4176 and CVE-2026-13221 (CRITICAL). CVE-2026-8376 is
# 32-bit-only and does not affect our x86_64 build.
url    = "https://www.cpan.org/src/5.0/perl-5.40.4.tar.xz"
sha256 = "eb0b1116b84ded552f686c9ee40c83000d8388c454f54197ca1d3a3b17246bd3"

[steps]
build = '''
cd "$name-$version";
./Configure -des \
  -Dprefix=/usr -Dvendorprefix=/usr \
  -Dprivlib=/usr/share/perl5/core_perl \
  -Darchlib=/usr/lib/perl5/core_perl \
  -Dsitelib=/usr/share/perl5/site_perl \
  -Dsitearch=/usr/lib/perl5/site_perl \
  -Dvendorlib=/usr/share/perl5/vendor_perl \
  -Dvendorarch=/usr/lib/perl5/vendor_perl \
  -Dman1dir=/usr/share/man/man1 -Dman3dir=/usr/share/man/man3 \
  -Dusethreads -Duseshrplib -Dinc_version_list=none \
  -Accflags='-std=gnu17 -fwrapv -fno-strict-aliasing';
make
'''
package = '''
cd "$name-$version";
make DESTDIR="$pkgdir" install;
install -Dm644 Copying "$pkgdir/usr/share/licenses/$name/Copying";
install -Dm644 Artistic "$pkgdir/usr/share/licenses/$name/Artistic"
'''
