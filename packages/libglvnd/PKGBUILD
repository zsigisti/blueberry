# Maintainer: Blueberry Linux <packages@blueberry.linux>
pkgname=libglvnd
pkgver=1.7.0
pkgrel=1
pkgdesc='The GL Vendor-Neutral Dispatch library (EGL/GLES dispatch for Wayland)'
arch=('x86_64')
url='https://gitlab.freedesktop.org/glvnd/libglvnd'
license=('custom')
depends=('glibc')
# GLX/X11 dispatch is disabled until the libxcb/libX11 sub-layer is packaged;
# EGL + GLES is all a Wayland session needs.
provides=('libGLdispatch.so' 'libEGL.so' 'libGLESv1_CM.so' 'libGLESv2.so' 'libOpenGL.so')
makedepends=('meson' 'ninja' 'python')
source=("https://gitlab.freedesktop.org/glvnd/libglvnd/-/archive/v$pkgver/libglvnd-v$pkgver.tar.gz")
sha256sums=('2b6e15b06aafb4c0b6e2348124808cbd9b291c647299eaaba2e3202f51ff2f3d')

build() {
  arch-meson "libglvnd-v$pkgver" build \
    -D x11=disabled -D glx=disabled -D egl=true -D gles1=true -D gles2=true
  meson compile -C build
}

package() {
  meson install -C build --destdir "$pkgdir"
  install -Dm644 "libglvnd-v$pkgver/README.md" \
    "$pkgdir/usr/share/licenses/$pkgname/README.md"
}
