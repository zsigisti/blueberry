# Maintainer: Blueberry Linux <blueberry@mmzsigmond.me>
pkgname=lib32-libxrender
pkgver=0.9.12
pkgrel=1
pkgdesc='X Rendering Extension library (32-bit)'
arch=('x86_64')
url='https://www.freedesktop.org/'
license=('custom')
depends=('lib32-glibc' 'lib32-libx11' )
makedepends=('lib32-glibc' 'lib32-gcc-libs' 'xorgproto' 'xorg-util-macros' 'pkgconf' 'lib32-libx11' )
options=('!lto' '!strip')
source=("libXrender-0.9.12.tar.xz::https://www.x.org/releases/individual/lib/libXrender-0.9.12.tar.xz")
sha256sums=('b832128da48b39c8d608224481743403ad1691bf4e554e4be9c174df171d1b97')

build() {
  export CC="gcc -m32" CXX="g++ -m32" PKG_CONFIG_PATH=/usr/lib32/pkgconfig
  cd "libXrender-0.9.12"
  ./configure --prefix=/usr --libdir=/usr/lib32 --host=i686-pc-linux-gnu --disable-static 
  make
}

package() {
  cd "libXrender-0.9.12"; make DESTDIR="$pkgdir" install
  rm -rf "$pkgdir/usr/include" "$pkgdir/usr/share" "$pkgdir/usr/bin"
}
