# bpm repositories — one line per repo: "<name> <url> [mirror2 mirror3 ...]"
# (blank lines and #comments ignored).
#
# Each url serves a `bpm.index` (built by tools/mkrepo.sh / blueberry-repo-sync)
# alongside the .pkg.tar.zst files. Both http:// and https:// work. Extra urls
# are MIRRORS: `bpm update` and downloads try each in turn and fail over.
#
# `$arch` expands to the machine's architecture (x86_64 / aarch64), so one
# config serves every arch and an aarch64 box can never pull x86_64 packages —
# each arch is published under its own subdir by blueberry-repo-sync.
#
# Example with a LAN mirror:
#   core https://repo.mmzsigmond.me/$arch http://192.168.0.97:81/$arch

core https://repo.mmzsigmond.me/$arch
